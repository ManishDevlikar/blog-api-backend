
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# enabling multipart upload
spring.servlet.multipart.enabled=true

#max file size
#spring.servlet.multipart.max-file-size=300MB

spring.servlet.multipart.max-request-size=10MB
#threshold after which files are written to disk
#spring.servlet.multipart.file-size-threshold=1KB
project.image=images/

logging.level.org.springframework.security=DEBUG
#spring.security.user.name=manish
#spring.security.user.password=manish
#spring.security.user.roles=ADMIN

springdoc.packagesToScan=com.api.blog.controllers
springdoc.pathsToMatch=/api/users/**,/api/**,/api/comment/**,/api/category/**,/api/auth/**

spring.profiles.active=prod
